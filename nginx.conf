server {
    listen 80;
    server_name localhost;  # No need for domain, as host Nginx handles it
    root /usr/share/nginx/html;  # React app build output
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;  # Serve React app (single-page app routing)
    }
}